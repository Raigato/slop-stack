/**
 * All app routes that must pass axe-core accessibility checks.
 * When adding a new page/route, add its path here.
 */
export const PAGES = [{ path: '/', name: 'landing' }] satisfies { path: string; name: string }[]
